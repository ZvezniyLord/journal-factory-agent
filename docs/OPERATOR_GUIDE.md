# Operator Guide

Run preflight first. If it says `BUILD BLOCKED`, inspect `build/reports/preflight.json`.

Run a draft build only for inspection while blockers remain. The release gate will not pass until mandatory files are available.

The admin panel exposes:

- current status;
- archive inventory;
- article audit;
- release gate;
- report links.

